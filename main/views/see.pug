extends ./layouts/base 
include ./mixins/video

block content 
    div.profile__container
        div.profile__image
            if(user.avatarUrl === "0") 
                span 😊
            else 
                img(src="/" + user.avatarUrl)
        div.profile__name=user.name
        div.video__grid 
            each video in user.videos  
                +video(video)
                