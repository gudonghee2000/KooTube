extends ./layouts/base
include ./mixins/video

block content
    div.video__grid
        each video in videos 
            +video(video)
           
        else
            div.empty__message
                span 비디오가 없습니다.